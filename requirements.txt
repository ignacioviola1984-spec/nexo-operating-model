# Runtime dependencies for deploying the Nexo v3 demo (Streamlit Community Cloud
# reads this file). Runtime only - no dev/test tools. Mirrors the locked set in
# requirements-v3.txt. The full pinned set (incl. dev) lives in requirements-v3.txt;
# install that for local development: pip install -r requirements-v3.txt
#
# No cloud SDKs: the data path is local (uploaded Excel -> local DuckDB snapshot).

anthropic==0.113.0
pydantic==2.13.4
pydantic-settings==2.14.2
streamlit==1.58.0
pandas==3.0.3
openpyxl==3.1.5
duckdb==1.5.4
pyarrow==24.0.0
python-dotenv==1.2.2
structlog==26.1.0
bcrypt==5.0.0
